{% extends ':Layout:layout.html.twig' %}

{% set module = 'playbook' %}

{% block content_title %}
    Playbooks
{% endblock %}

{% block content_body %}
    <div class="box box-primary">
        <div class="box-header">
            {% if entity is null %}New Playbook{% else %}{{ entity.name }}{% endif %}
        </div>
        <div class="box-body">
            {{ form_start(form) }}

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}